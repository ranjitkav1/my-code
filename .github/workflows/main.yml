name: Run my first job
on: [push]
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: make script executable
        run: chmod u+x hello_world.sh
      - name: run the script
        run: ./hello_world.sh
      
