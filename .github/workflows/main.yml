name: Run my first job
on: [push]
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: print, change and list current directory
        run: |
          CURRDIR=$(pwd)
          echo "$CURRDIR"
          cd .
          cd -
          cd -
          ls -la
      
