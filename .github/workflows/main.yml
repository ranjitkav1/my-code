name: Run my first job
on: [push]
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: make script executable
        run: chmod u+x setupenv.sh
      - name: run the script
        run: ./setupenv.sh
      
